# Ejemplo de variables de entorno para Lingua Avventura
# Copia como .env.local (desarrollo) o define como secrets en GitHub Actions (producción)

# --- Supabase ---
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi... (anon key pública)
# Tabla opcional personalizada para progreso (por defecto user_progress)
# VITE_SUPABASE_PROGRESS_TABLE=user_progress

# --- Vocabulario ---
# API REST opcional (fallback si falla Supabase) — servidor local Node
# VITE_PACKS_API_URL=http://localhost:4000

# Puedes añadir otras banderas personalizadas aquí
# VITE_FEATURE_X=true
